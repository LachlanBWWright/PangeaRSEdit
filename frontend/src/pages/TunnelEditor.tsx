/**
 * Tunnel Editor Page
 *
 * Route component for the Bugdom 2 tunnel level editor.
 */

import { TunnelEditor } from "@/editor/tunnel";

export function TunnelEditorPage() {
  return (
    <div className="h-full bg-gray-900">
      <TunnelEditor />
    </div>
  );
}
