/**
 * Topology Preview 3D - DISABLED
 * 
 * This component no longer shows a full terrain preview or any overlay.
 * The main visual feedback is provided by TopologyBrush3D (the green circle/square indicator).
 * Terrain modifications apply directly to the heightmap without preview layers.
 */
export function TopologyPreview3D() {
  // Preview disabled - return null to keep interface clean
  // Visual feedback provided by TopologyBrush3D component only
  return null;
}
