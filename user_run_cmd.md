# Manual commands to run

The following commands were prepared for you to run outside this session:

```bash
cd /home/lachl/documents/pangearsedit/games/pangea-ports
sudo apt-get update
sudo apt-get install -y liblua5.4-dev
```

If you want to verify the install afterward:

```bash
pkg-config --cflags --libs lua5.4
```
